info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: http://{{base_url}}/users/{{user_email}}
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
