info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/users/newuser@example.com"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
