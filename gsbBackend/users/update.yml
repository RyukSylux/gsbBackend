info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: http://{{base_url}}/users/{{user_email}}
  body:
    type: json
    data: |-
      {
        "name": "Updated Name",
        "description": "Updated description",
        "currentPassword": "password123",
        "newPassword": "newpassword456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
