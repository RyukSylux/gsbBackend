info:
  name: get all
  type: http
  seq: 1

http:
  method: GET
  url: http://{{base_url}}/users
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
