info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      {
        "name": "New User",
        "email": "newuser@example.com",
        "password": "password123",
        "description": "I am a new user"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
