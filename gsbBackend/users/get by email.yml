info:
  name: get by email
  type: http
  seq: 2

http:
  method: GET
  url: http://{{base_url}}/users/?email=ryuk@gmail.com
  params:
    - name: email
      value: ryuk@gmail.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
