info:
  name: getStats
  type: http
  seq: 1

http:
  method: GET
  url: http://{{base_url}}/bills/stats
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
