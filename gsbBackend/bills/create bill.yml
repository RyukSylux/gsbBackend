info:
  name: create bill
  type: http
  seq: 6

http:
  method: POST
  url: http://{{base_url}}/bills
  body:
    type: multipart-form
    data:
      - name: metadata
        type: text
        value: '{"date":"27/10/2025","amount":150,"description":"Test bill","status":"pending","category":"Transport"}'
      - name: proof
        type: text
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
