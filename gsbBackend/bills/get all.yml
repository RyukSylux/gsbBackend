info:
  name: get all
  type: http
  seq: 2

http:
  method: GET
  url: http://{{base_url}}/bills
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
