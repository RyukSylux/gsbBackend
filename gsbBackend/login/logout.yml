info:
  name: logout
  type: http
  seq: 2

http:
  method: POST
  url: http://{{base_url}}/login/logout
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
