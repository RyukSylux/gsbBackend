info:
  name: connect user
  type: http
  seq: 4

http:
  method: POST
  url: http://{{base_url}}/login
  body:
    type: json
    data: |-
      {
        "email":"hugo@gmail.com",
        "password":"hugo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
